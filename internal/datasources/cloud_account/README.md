# `data.nc2_cloud_account`

Fetches a single NC2 cloud account by id. Credentials are NOT
returned (NC2 hides them).

## OpenAPI coverage

`GET /cloud-accounts/{id}` →
`CPanelWeb.Api.CloudAccountController.show`.
