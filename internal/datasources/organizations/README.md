# `data.nc2_organizations`

Lists every NC2 organization visible to the configured credentials.
Sorted ascending by `id` (FR-015).

## OpenAPI coverage

`GET /organizations` →
`CPanelWeb.Api.OrganizationController.index`.
