# `data.nc2_organization_audit_trail`

Lists audit-trail entries recorded for an NC2 organization. Sorted
ascending by `id` (FR-015).

## OpenAPI coverage

`GET /organizations/{id}/audit-trails` →
`CPanelWeb.Api.OrganizationController.list_audit_trails`.
