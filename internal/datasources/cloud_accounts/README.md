# `data.nc2_cloud_accounts`

Lists every NC2 cloud account belonging to the supplied
organization. Sorted ascending by `id` (FR-015).

## OpenAPI coverage

`GET /organizations/{id}/cloud-accounts` →
`CPanelWeb.Api.OrganizationController.list_cloud_accounts`.
