# `data.nc2_cloud_account_regions`

Lists every region enabled on an NC2 cloud account. Sorted ascending
by `id` (FR-015).

## OpenAPI coverage

`GET /cloud-accounts/{cloud_account_id}/regions` →
`CPanelWeb.Api.RegionController.index`.
