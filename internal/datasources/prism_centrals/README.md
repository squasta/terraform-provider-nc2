# `internal/datasources/prism_centrals`

Implements `data.nc2_prism_centrals` — sorted-by-`id` list of Prism
Central instances inside a single cloud-account region.

Covers `CPanelWeb.Api.PrismCentralController.index`.
