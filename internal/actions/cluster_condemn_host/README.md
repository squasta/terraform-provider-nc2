# `internal/actions/cluster_condemn_host`

Implements `nc2_cluster_condemn_host` — issues `POST
/clusters/{id}/condemn-host` and polls the resulting task.

Covers `CPanelWeb.Api.ClusterController.condemn_host`.
